spring:
  application:
    name: restapi

  datasource:
    driver-class-name: org.postgresql.Driver
    password: root
    username: root
    url: "jdbc:postgresql://localhost:5432/default_database"
  jpa:
    hibernate:
      ddl-auto: update
  data:
    jdbc:
      dialect: postgresql